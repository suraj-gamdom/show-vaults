{
  "name": "fetch-vault",
  "version": "0.1.0",
  "private": true,
  "description": "Simple API to fetch vault_ids_to_hide.json from a GET API response",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
